#import "DSListController.h"

// Reached from the About item in the navigation bar: what the tweak is, where it
// came from, and the escape hatch if a build ever misbehaves.
@interface DSAboutListController : DSListController
@end
