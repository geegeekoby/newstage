#import "DSListController.h"

// Per-application page: disabled, launch mode, and the two switches that only
// mean anything once an app runs in iPad mode.
@interface DSAppBehaviorController : DSListController
@end
