#import "DSPrefsPrivate.h"

// Slider row that prints its own value as a percentage on the trailing edge,
// the way the stock Stage scale row does.
@interface DSScaleSliderCell : PSSliderTableCell
@end
